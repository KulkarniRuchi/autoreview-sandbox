import os  # TRIVIAL: unused import
import math

def add_numbers(x, y):  # TRIVIAL: unclear naming
    return x + y

def multiply(first_number, second_number):
    return first_number * second_number

def circle_area(r):
    return math.pi * r * r